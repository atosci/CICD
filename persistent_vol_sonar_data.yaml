apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarq-volume-data
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 10Mi