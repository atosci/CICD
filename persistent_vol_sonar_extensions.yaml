apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarq-volume-extensions
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 10Mi            